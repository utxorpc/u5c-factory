# CLAUDE.md

This repository's canonical agent guidance lives in [`AGENTS.md`](./AGENTS.md)
at the repo root. Read it first.

Component-specific guidance lives inside each submodule (`spec/`, `sdks/*`).
Do not duplicate guidance here — update `AGENTS.md` instead.
